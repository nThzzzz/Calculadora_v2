#include "manip_matrizes.h"

// compilar
//  gcc -o calcmatriz main.c manip_matrizes.c
//  ./calcmatriz

int main() {
  pessoa pessoas[10];
  int verificacao;
  int usuariologado = -1;
  printf("%d\n", verificacao);
  menuinicial(pessoas, usuariologado);

  return 0;
}
